// Package adapter provides small adapters between external storage services
// (e.g., viant/afs) and Go's standard io/fs interfaces so that loaders can
// operate against arbitrary backends.
package adapter
