// Package transform provides a small, composable layer to apply structural
// rewrites to syntetic/model graphs without polluting the core model or the
// builders. It defines a Transformer interface and helpers to compose and
// apply transformations to packages, types, and nodes.
package transform
